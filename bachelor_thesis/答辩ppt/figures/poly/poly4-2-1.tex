\documentclass[border={-10pt 0pt 0pt 0pt}]{standalone}
\usepackage{tikz-cd,tikz-3dplot} 
\usetikzlibrary{shapes.geometric, arrows, backgrounds,external,decorations.pathmorphing, decorations.shapes,decorations.markings}

%include other needed packages here   
\begin{document}	
	\makeatletter
	\tikzset{nomorepostaction/.code={\let\tikz@postactions\pgfutil@empty}}
	\makeatother
	\tdplotsetmaincoords{65}{100}
	\begin{tikzpicture}[tdplot_main_coords, scale=2, line join=round,
		one/.style={
			postaction={nomorepostaction,decorate,
				decoration={markings,mark=at position 0.7 with {\arrow[very thick]{Latex}}}
			}
		},
		two/.style={
		postaction={nomorepostaction,decorate,
			decoration={markings,mark=at position 0.7 with {\arrow[very thick]{Latex}}},
			decoration={markings,mark=at position 0.6 with {\arrow[very thick]{Latex}}}
		}
	}
	]
	\pgfmathsetmacro\a{0.5}
	\pgfmathsetmacro{\phi}{\a*(1+sqrt(5))/2}
	\pgfmathsetmacro{\phd}{\a*(3+sqrt(5))/2}
	\pgfmathsetmacro{\az}{\a* sqrt((3*((1+sqrt(5))/2)+6)/5)}
	\pgfmathsetmacro{\ax}{\az*(1+sqrt(5))/2}
	\path 
	coordinate(A) at (\phi,\phi,\phi)
	coordinate(B) at (\phi,\phi,-\phi)
	coordinate(C) at (\phi,-\phi,\phi)
	coordinate(D) at (\phi,-\phi,-\phi)
	coordinate(E) at (-\phi,\phi,\phi)
	coordinate(F) at (-\phi,\phi,-\phi)
	coordinate(G) at (-\phi,-\phi,\phi)
	coordinate(H) at (-\phi,-\phi,-\phi)
	coordinate(I) at (0,\phd,\a)
	coordinate(J) at (0,\phd,-\a)
	coordinate(K) at (0,-\phd,\a)
	coordinate(L) at (0,-\phd,-\a)
	coordinate(M) at (\phd,\a,0)
	coordinate(N) at (\phd,-\a,0)
	coordinate(O) at (-\phd,\a,0)
	coordinate(P) at (-\phd,-\a,0)
	coordinate(Q) at (\a,0,\phd)
	coordinate(R) at (-\a,0,\phd)
	coordinate(S) at (\a,0,-\phd)
	coordinate(T) at (-\a,0,-\phd)
	coordinate(PO) at (\ax,0,\az)
	coordinate(MN) at (\phd,0,0)
	; 
	\draw[dashed, thick]    
	(PO) -- (A)
	(PO) -- (Q)
	(PO) -- (C)
	(A) -- (Q) -- (R) -- (E) -- (I) --cycle
	(A) -- (M) -- (B) -- (J) -- (I) --cycle
	(R) -- (G) -- (K) -- (C) -- (Q) --cycle
	(C) -- (N) -- (M) 
	(K) -- (L) -- (D) -- (N)
	(D) -- (S) -- (B)
	;
	\draw[ultra thick, fill=red!40]
	(M) -- (PO) -- (N) --cycle;
	\draw[one](MN) -- (N) ;
	\draw[one](MN) -- (M) ;
	\draw[two](N) -- (PO) ;
	\draw[two](M) -- (PO) ;
	\fill (MN) circle (1.5pt);
	\foreach \point/\position/\tip in {PO/{above right}/O,MN/below/c}
	{
		\fill (\point) circle (1.5pt);
		\node[\position=3pt] at (\point) {$\tip$};
	}
	\foreach \point/\position/\tip in {M/right/B,N/left/A}
{
	\fill (\point) [blue]circle (1.5pt);
	\node[\position=3pt,blue] at (\point) {$\tip$};
}
%	\foreach \point/\position in {A/right,B/below,C/above,D/left,E/{above right},F/below,G/above,H/left,I/below,J/right,K/below,L/left,M/left,N/left,O/left,P/left,Q/left,R/left,S/left,T/left,PO/left,MN/left}
%	{
%		\fill (\point) circle (1.5pt);
%		\node[\position=3pt] at (\point) {$\point$};
%	}
%	\begin{pgfonlayer}{background}
%	\filldraw [line width=3mm,join=round,black!10,opacity=0]
%	(R.north -| L.east) rectangle (S.south -| J.west);
%	\end{pgfonlayer}
	\end{tikzpicture}
\end{document}